# In this lets see about "File Handling in python."

# python "os module" is used to interact with the operating system.

# This "os module" is used to renaming , removing , deteling etc in file handling.

# mkdir( ) =  Used to create the directories in the current working directory. 

# Syntax for mkdir( ):

# mkdir( "DirName" )

# Here is the program for "mkdir( )".

import os

# making the directory Named NewDir.

os.mkdir( "NewDir" )

print ( "\nNewDir is created successfully by using the mkdir( )..." )
