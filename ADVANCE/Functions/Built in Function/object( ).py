# In this lets see about the "Built-in functions" in python.

# Built-in function is defined as the functions whose functionality is pre-defined in the python compiler.

# Lets see about "object( ) bilt-in function" in python.

# object( ) - Used to return an empty object.

# It is a base for all the classes and holds the built-in properties and methods which are default for all the classes.

# Here is the program for object( )

Object = object( )

print( ) # prints new line for readability.

print ( type( Object ) )

print( ) # prints new line for readability.

print ( dir( Object ) )

