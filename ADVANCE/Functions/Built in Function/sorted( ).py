# In this lets see about the "Built-in functions" in python.

# Built-in function is defined as the functions whose functionality is pre-defined in the python compiler.

# Lets see about "sorted( ) bilt-in function" in python.

# sorted( ) - Used to sort elements.

# Note : By default the elements are sorted in ascending order.

# Note : it sorts only the keys in case of dictionary datatype.

# Here is the program for sorted( ).

print ( )

sort = ( 8, 2, 3, 7, 6, 4, 9, 0, 1, 5 )

print( sorted( sort ) )

