# Lets see about random module.

# random module functions depend on a pseudo-random number generator function random(), which generates the float number between 0.0 and 1.0.

# random.choice( ) = used to return a random number from a list or sequence.

# Here is the example program for random.choice( )

import random

print('\nRandom number generated using choice( ) from list is :',random.choice([0, 10, 5, 20, 15, 30, 25]))

print( ) # prints empty line for readyability


