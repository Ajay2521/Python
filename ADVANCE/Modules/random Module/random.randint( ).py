# Lets see about random module.

# random module functions depend on a pseudo-random number generator function random(), which generates the float number between 0.0 and 1.0.

# random.randint( ) = used to return a random integer number between specified integers.

# Here is the example program for random.randint( )

import random

print('\nRandom number generated using randint( ) is :',random.randint(0, 10))

print( ) # prints empty line for readyability


